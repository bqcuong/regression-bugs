sudo: required
addons:
  chrome: stable

language: node_js
node_js:
- "node"

branches:
  only:
    - master

before_script:
  - cd frontend
  - npm install

script:
  - ng build --prod --aot
  - ng test